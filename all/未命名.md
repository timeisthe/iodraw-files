```mermaid
graph LR
    User(("用户操作")) -->|"Click"| View["View (Activity/Fragment)"]
    View -->|"调用"| VM["ViewModel"]
    VM -->|"请求"| Repo["Repository"]
    Repo -->|"IO操作"| Net["Remote / Local Data"]
    Net -->>|"数据返回"| Repo
    Repo -->>|"LiveData/Flow"| VM
    VM -->>|"LiveData/Flow (Observe)"| View
    View -->>|"Update"| UI(("更新 UI"))
```
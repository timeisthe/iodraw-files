```mermaid
graph TD
    Start[获取 ViewHolder] --> A{mChangedScrap <br/>(预布局动画用)}
    A -->|命中| Return[返回 Holder]
    A -->|未命中| B{mAttachedScrap <br/>(屏幕内复用)}
    B -->|命中| Return
    B -->|未命中| C{mCachedViews <br/>(刚移出屏幕)}
    C -->|命中| Return
    C -->|未命中| D{ViewCacheExtension <br/>(自定义)}
    D -->|命中| Return
    D -->|未命中| E{RecycledViewPool <br/>(类型池)}
    E -->|命中| Bind[rebindData<br/>执行 onBindViewHolder]
    Bind --> Return
    E -->|未命中| Create[createViewHolder<br/>执行 onCreateViewHolder]
    Create --> Bind
```